*** Settings ***
Library    DatabaseLibrary
Library    OperatingSystem

*** Variables ***
${DB_HOST}        your_server.database.windows.net
${DB_PORT}        1433
${DB_DATABASE}    your_database

*** Test Cases ***
Connect to Azure SQL Server with Active Directory Authentication
    ${connection_string}=    Set Variable    DRIVER={ODBC Driver 17 for SQL Server};SERVER=${DB_HOST},${DB_PORT};DATABASE=${DB_DATABASE};Trusted_Connection=yes
    Connect To Database    mssql    ${connection_string}
    @{results}=    Query    SELECT * FROM your_table
    Log Many    ${results}
    Disconnect From Database
